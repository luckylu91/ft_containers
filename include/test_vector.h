#pragma once
#include "test_all.h"
#ifdef MINE
  #include "vector.hpp"
  // using ft::pair;
  // using ft::make_pair;
  // using ft::map;
  using namespace ft;
#else
  #include <vector>
  // using std::pair;
  // using std::make_pair;
  // using std::map;
  using namespace std;
#endif
#include "test_utils.h"
